# Dependencias para detección de objetos con YOLO
# Crear entorno virtual: python3 -m venv venv
# Activar: source venv/bin/activate
# Instalar: pip install -r requirements-yolo.txt

ultralytics>=8.3.0
opencv-python>=4.12.0
torch>=2.8.0
torchvision>=0.23.0
pillow>=11.3.0
numpy>=2.2.0
fastapi>=0.118.0
uvicorn>=0.37.0
picamera2