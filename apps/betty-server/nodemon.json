{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "dist"],
  "exec": "ts-node --project tsconfig.app.json --transpile-only src/main.ts",
  "env": {
    "NODE_ENV": "development"
  },
  "delay": 500
}
