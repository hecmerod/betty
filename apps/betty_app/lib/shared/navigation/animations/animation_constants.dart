/// Duración de las animaciones de hide/show de widgets
const Duration kHideWidgetAnimationDuration = Duration(milliseconds: 300);

/// Buffer adicional para asegurar que la animación termine antes de continuar
const Duration kHideWidgetAnimationBuffer = Duration(milliseconds: 50);

/// Duración total incluyendo el buffer
const Duration kHideWidgetAnimationTotal = Duration(milliseconds: 350);
